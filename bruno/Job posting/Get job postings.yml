info:
  name: Get job postings
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/job-postings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
