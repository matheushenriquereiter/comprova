info:
  name: User sign up
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/auth/sign-up"
  body:
    type: json
    data: |-
      {
        "username": "matheus",
        "email": "mathenriquereiter@gmail.com",
        "password": "matheus123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
